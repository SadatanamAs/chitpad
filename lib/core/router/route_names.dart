/// All route name constants in one place.
/// Always use these — never hardcode strings in Get.toNamed()
abstract class RouteNames {
  static const String splash    = '/';
  static const String login     = '/login';
  static const String register  = '/register';
  static const String notesList = '/notes';
  static const String noteDetail = '/notes/detail';

  // TODO: Add routes as you build features
}
